const styles = {
  title: {
    textAlign: 'center',
    marginTop: '100px',
    fontSize: '50px',
  },
};

const HomePage = () => (
  <h1 style={styles.title}>Welcome to your contacts app :)</h1>
);

export default HomePage;
